version: "3.7"

services:
  backend-test:
    build: ./backend
    working_dir: /backend
    container_name: backend-test
    command: pytest -v
    volumes:
      - ./backend/:/backend
    env_file:
      - .env
    environment:
      - STAGE=test
